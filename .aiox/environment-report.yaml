# AIOX Environment Bootstrap — Environment Report
# Generated by @devops (Gage) on 2026-04-29

generated_at: "2026-04-29"
project_name: transcricao
project_owner: andre (versalhes-cohort)

system:
  os: Linux (WSL2)
  kernel: 6.6.87.2-microsoft-standard-WSL2
  architecture: x86_64
  shell: bash
  host: Violeta

cli_tools:
  essential:
    git:
      installed: true
      version: 2.43.0
      path: /usr/bin/git
    gh:
      installed: true
      version: 2.88.1
      authenticated: true
      account: versalhes-cohort
      scopes:
        - gist
        - read:org
        - repo
        - workflow
    python3:
      installed: true
      version: 3.12.3
      meets_prd_requirement: true  # PRD requires 3.11+
    node:
      installed: true
      version: 20.20.0

  infrastructure:
    railway:
      installed: true
      version: 4.37.4
      authenticated: unknown  # not checked during bootstrap; user will run `railway login` before deploy in Story 1.7
    docker:
      installed: false
      note: "Not required — Railway builds the Dockerfile remotely. Local Docker only needed if user wants to test image build locally."

  optional:
    coderabbit:
      installed: unknown
      note: "WSL-based install required. Will be set up by @qa during QA Loop phase if needed."
    supabase:
      installed: not_checked
      note: "Not required for this project (no database per NFR10)."

repository:
  initialized: true
  initial_commit: 1643dc95d52a6f324c86df99e3de6f24ee4aa4b0
  initial_commit_message: "chore: initial project setup"
  branch: main
  remote_url: https://github.com/versalhes-cohort/transcricao
  visibility: public
  files_committed: 3421
  insertions: 1179505

security:
  gitignore_audit: passed
  secrets_in_env_file: 15  # OPENAI_API_KEY, GITHUB_TOKEN, etc.
  env_file_gitignored: true
  env_example_committed: true
  scan_for_leaked_keys: passed  # only false-positive in test fixtures

project_structure:
  docs:
    - docs/brief.md (analyst v1.0)
    - docs/prd.md (PM v1.0)
    - docs/architecture.md (architect v1.1)
  code:
    not_yet_implemented: true
    note: "app/ tree will be created by @dev during Story 1.1 implementation"
  framework:
    aiox_core_committed: true
    aiox_version: 2.x
    install_pattern: project-development

validation:
  essential_complete: true
  github_repo_created: true
  github_repo_url: https://github.com/versalhes-cohort/transcricao
  push_succeeded: true
  ready_for_story_drafting: true

next_steps:
  immediate:
    - "@sm (River) drafts Story 1.1 via *draft (uses architecture.md §2 as deployment topology source)"
    - "@po (Pax) validates Story 1.1 draft via *validate-story-draft"
    - "@dev (Dex) implements Story 1.1 via *develop"
  before_story_1_7_launch:
    - "User must set OpenAI account spending cap to USD $20/month manually (NFR3)"
    - "User must run `railway login` before deploy (Railway CLI auth)"
    - "User must add OPENAI_API_KEY to Railway dashboard env vars"

bootstrap_completed_by: "@devops (Gage)"
